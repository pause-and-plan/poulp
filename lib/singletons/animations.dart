class SwapAnimations {
  Duration successDuration = const Duration(milliseconds: 200);
  Duration failDuration = const Duration(milliseconds: 200);
}

SwapAnimations swapAnimations = SwapAnimations();
